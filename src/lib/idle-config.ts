/** Tempo sem interação antes da hibernação (2 min). */
export const IDLE_TIMEOUT_MS = 2 * 60 * 1000;

/** Duração fixa de cada slide de imagem no catálogo (5 s). */
export const IMAGE_SLIDE_MS = 5000;
